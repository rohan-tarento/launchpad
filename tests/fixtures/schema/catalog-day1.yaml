apiVersion: launchpad/v1
kind: ServiceCatalog
org: apex-common

services:
  kola-meta:
    stack: meta-pm
    description: Control-plane for KOLA programme
    status: live
    teams: [platform-core]
    links:
      repo: https://github.com/apex-common/kola-meta

  # ─── Day-N examples (remove the '#' when the repo is live) ──────────────
  #
  # kola-platform-core:
  #   stack: python-backend
  #   description: Core platform microservice
  #   status: planned
  #   teams: [platform-core]
  #
  # kola-hitl-web:
  #   stack: nextjs-frontend
  #   description: Human-in-the-loop web UI
  #   status: planned
  #   teams: [hitl-dev]
  #
  # kola-infra:
  #   stack: terraform-iac
  #   description: Terraform modules for KOLA infrastructure
  #   status: planned
  #   teams: [platform-core]
