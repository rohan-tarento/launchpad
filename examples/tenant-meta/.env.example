# Copy to .env in your tenant <client>-meta repo (never commit .env)

# GitHub fine-grained PAT (factory automation)
GITHUB_TOKEN=github_pat_REPLACE_ME

# GitLab (roadmap — phase 6)
# GITLAB_TOKEN=glpat-REPLACE_ME
# GITLAB_HOST=https://gitlab.com
